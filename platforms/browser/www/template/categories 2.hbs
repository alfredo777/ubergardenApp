{{#each categorias}}

  <div class="category">
  <a onclick="search_result('{{search}}');">
  {{#if img}}
  <img src="{{#host img}}{{/host}}" width="100%">
  {{/if}}
  {{name}}
  </a>
  </div>
{{/each}}

